const nome = 'Ismael'; // string.
const num = 10; // number.
let nome1; // undefined = valor nulo.
let nome2 = null; // nulo, não são iguais. 
const boolean = true; // true ou false (lógico).

console.log(typeof nome, nome); // ver tipo.